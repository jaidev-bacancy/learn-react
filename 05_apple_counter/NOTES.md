# What I did?

 - Recreated the apple counter mechanism using React.

# What I learnt?
- Event handling
- After adding the event listeners, the component did not re-rendered and so the changes weren't reflected. Hence the need of state management (using Hooks).
- **Note:** I have used root.render() in the event handlers for learning purpose only.